#include <bits/stdc++.h>
using namespace std;

int main() {
  int H, W;
  scanf("%d%d", &H, &W);

  string pixel;
  for (int i = 0; i < H; i++) {
    cin >> pixel;
    cout << pixel << endl;
    cout << pixel << endl;
  }
  return 0;
}

